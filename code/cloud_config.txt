# Ansel Lim, ansel@gatech.edu
# 24, 25, 26 Nov 2021

##### HOW TO RUN ".\add_quality_data.py" ON CLOUD #####

# APT-GET UPDATE, INSTALL SCREEN.
sudo apt-get update
sudo apt-get install screen

# DOWNLOAD REPOSITORY (enter credentials to download private repository)
sudo apt-get install git
git clone https://github.com/ansellim/6242.git

# INSTALL CONDA
sudo apt install curl
curl -O https://repo.anaconda.com/archive/Anaconda3-2020.02-Linux-x86_64.sh
bash Anaconda3-2020.02-Linux-x86_64.sh
source ~/anaconda3/bin/activate

# CONFIGURE CONDA
conda create -n myenv numpy pandas requests
conda activate myenv
pip install python-dotenv geopy sklearn

# CD INTO CODE FOLDER
cd 6242/code

# ADD .ENV FILE WITH GOOGLE API KEY
echo "GOOGLE_API_KEY=<YOUR API KEY HERE>" > ../.env

# RUN SCRIPT
screen
conda activate myenv
python add_quality_data.py

# TO DETACH SCREEN, JUST CTRL + A + D. TO RE-ENTER SCREEN, type "screen-r"
