name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "8b92d298-eea4-497b-bc2e-3d94fb49af36"
          heroku_app_name: "apartments-recommendation" #Must be unique in Heroku
          heroku_email: "ansellim@gmail.com"